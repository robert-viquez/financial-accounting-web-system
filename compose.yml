services:
  faws:
    image: ${FAWS_IMAGE:-ghcr.io/robert-viquez/financial-accounting-web-system:latest}
    build: .
    restart: unless-stopped
    ports:
      - "${FAWS_PORT:-8080}:80"
    volumes:
      - faws_data:/data

volumes:
  faws_data:
